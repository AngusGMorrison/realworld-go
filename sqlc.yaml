version: "2"
sql:
  - schema: "internal/repository/sqlite/migrations"
    queries: "internal/repository/sqlite/queries"
    engine: "sqlite"
    gen:
      go:
        out: "internal/repository/sqlite/sqlc"
    database:
      uri: "${REALWORLD_DATA_DIR}/${REALWORLD_DB_BASENAME}"
    rules:
      - sqlc/db-prepare
